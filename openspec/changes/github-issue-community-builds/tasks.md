## 1. 投稿协议与共享校验

- [x] 1.1 新增社区投稿模块：版本化信封、唯一 JSON 块解析/生成、严格字段和长度校验、稳定 Issue ID、可信来源链接及作者映射。
- [x] 1.2 将手动编辑器的配装合法性检查提取为共享纯函数，编辑器和 Node 同步脚本复用，保留现有导入迁移兼容行为。
- [x] 1.3 实现投稿最低完整度、版本差异警告和诊断；先校验原始数据再处理默认值，不截断投稿。
- [x] 1.4 验证投稿往返保留全部词条组合、Hash、插槽与中文备注；覆盖畸形/超限数据、伪造作者和危险文本不执行。

## 2. 静态快照同步

- [x] 2.1 实现可使用本地夹具或 GitHub API 的同步脚本，读取全部分页与关闭 Issue，排除 Pull Request，从当前状态判断发布资格。
- [x] 2.2 实现开启、build、合法、未屏蔽四项交集；输出单个版本化快照和最少状态诊断，隐藏内容不进入产物。
- [x] 2.3 实现分页/限流/依赖失败时终止发布、单条非法时隔离、纯生成目录重建以及稳定排序和来源版本记录。
- [x] 2.4 验证完整状态转换矩阵，特别是屏蔽后编辑/重开、关闭后解除屏蔽、移除 build、修改为非法数据、删除与转移、乱序事件和第二页失败。

## 3. 当前仓库的投稿与 CI

- [x] 3.1 将投稿模板纳入当前仓库，仅预置 build；现有 Pages CI 监听所需 Issue 事件并提供手动、每日对账入口。
- [x] 3.2 工作流检出默认分支并记录实际 SHA，同一 checkout 提供校验器和目录；初始化 job 仅创建缺失标签定义，同步 job 只读 Issue，共用并发组串行发布。
- [x] 3.3 编写部署与维护说明：当前仓库与同源数据 URL、标签初始化和协作者权限、Pages CI、缓存和定时任务恢复、失败重跑及回退。

## 4. 社区浏览与详情

- [x] 4.1 新增前端仓库/快照配置与示例，将 usePublicBuilds 接入经过格式校验的快照，提供加载、失败、重试、未配置和生成时间。
- [x] 4.2 调整列表展示模型，支持社区 Hash 装备预览、作者、关键词/职业/活动筛选，保留编辑推荐来源兼容。
- [x] 4.3 新增社区详情路由和完整手动配装展示，提供原 Issue、复制导入及管理入口，不生成编辑验证结论。
- [x] 4.4 实现下架/屏蔽/删除详情不可用状态及刷新清除旧内容，修正社区空态和实机验证文案。

## 5. 编辑器投稿交接

- [x] 5.1 增加投稿元信息和预览、复制内容、手动复制兜底及 GitHub 模板跳转，长 JSON 不进入 URL。
- [x] 5.2 增加社区导入、创建独立副本与修改原投稿模式，保存来源编号和版本，链接回原 Issue 并提示权限及版本冲突。
- [x] 5.3 提供关闭/重开操作指引、同步等待与刷新提示；编辑器未配置社区时仍可本地使用。

## 6. 验证与上线

- [x] 6.1 运行投稿/同步相关测试、既有配装测试与生产构建，确认共享校验提取没有改变已有配装行为。
- [x] 6.2 使用本地快照进行浏览器验证：移动端和桌面投稿、复制失败、筛选、社区详情、完整导入、编辑原稿、刷新下架及网络错误。
- [x] 6.3 验证当前仓库的 CI 配置和同源打包：自动配置仓库/数据地址，生成快照并随网站一次发布，移除独立仓库模板。
- [ ] 6.4 推送当前实现并确认现有仓库 Pages CI 成功，验证真实 Issue 关闭/重开及屏蔽的发布结果。

实施验证记录：137 项 Node 测试、内容校验、构筑审计、生产构建、工作流 YAML 解析均通过。桌面与 390px 手机浏览器验证了投稿/编辑原稿的无损传递、剪贴板失败、原稿冲突、下架清除、错误快照和未配置状态，无运行时 JavaScript 错误。

上线待办：CI 使用内置 GITHUB_TOKEN，不依赖本机 gh 登录。仍需将本地实现与配套目录推送至当前仓库默认分支，并确认远端 Actions/Pages 成功执行；不创建新仓库。

同仓库调整验证：8 项社区回归测试通过；工作流校验覆盖标签初始化幂等、读写权限隔离、初始化跳过时继续部署和串行发布；按 CI 环境生产构建通过。通过本地 HTTPS 验证项目子路径、同源 JSON 加载、当前仓库详情链接与投稿跳转。
