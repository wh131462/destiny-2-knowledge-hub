# 每周轮换同步使用的 Bungie API Key，仅供 Node.js 脚本读取。
# 复制为仓库根目录的 .env.local（或 .env），然后填写 Key。
# 优先级：系统环境变量 > .env.local > .env。
# 填好后运行 npm run dev，启动前会自动同步每周轮换。
# 不要添加 VITE_ 前缀，也不要将真实 Key 提交到 Git。
BUNGIE_API_KEY=
