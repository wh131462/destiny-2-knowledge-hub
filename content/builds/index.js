// Public build catalog.
//
// Editorial examples were intentionally removed until they have reproducible
// in-game evidence. Community submissions can be added here later, or loaded
// through the public-build source boundary in the web app.
export const curatedBuilds = []
export const buildById = Object.create(null)
